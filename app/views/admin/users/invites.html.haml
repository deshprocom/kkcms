%div
  %table.index_table
    %thead
      %tr
        %th 头像
        %th 昵称
        %th 手机号
        %th 是否有完成任务
        %th 注册时间
    %tbody
      - @relation_users.includes(:user).each do |item|
        %tr
          %td= avatar_profile(item.user, size: 60)
          %td= item.user.nick_name
          %td= item.user.mobile
          %td= item.new_user ? '已完成' : '未完成'
          %td= item.created_at
  %div.index_foot
    = paginate(@relation_users, remote: true)